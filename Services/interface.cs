using System.Collections.Generic;

namespace crecer_backend.Services
{
    public class Iinterface
    {
        /*
        public interface ILoginService
        {
            bool VerifyUserExist(string rut);
            bool VerifyUserAndPassword(LoginDto user);
            UserDto GetUser(string rut);
            IEnumerable<ProgramaDto> GetUserPrgs(string rut);
            IEnumerable<CompañiaDto> GetUserCias(string rut);
        }
        */
    }
}
